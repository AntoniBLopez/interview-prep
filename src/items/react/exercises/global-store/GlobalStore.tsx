/**
 * Objetivo: estado global accesible desde varios componentes.
 *
 * - Crea un store/context con Provider
 * - Consume el estado desde al menos dos componentes hijos
 * - Compara mentalmente: Context API vs Redux / Zustand / Jotai
 */
export function GlobalStore() {
  return (
    <div className="flex flex-col gap-4">
      {/* Implementa aquí */}
    </div>
  )
}

/**
 * Plantilla para nuevos ejercicios.
 *
 * 1. Copia esta carpeta y renómbrala (ej: `use-ref/`)
 * 2. Implementa tu componente exportado
 * 3. Regístralo en `src/items/react/exercises/registry.ts` (incluye el campo `question`)
 */
export function TemplateExercise() {
  return (
    <div className="flex flex-col gap-4">
      {/* Implementa aquí */}
    </div>
  )
}

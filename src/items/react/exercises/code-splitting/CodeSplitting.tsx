/**
 * Objetivo: code splitting con React.lazy + Suspense.
 *
 * - Carga un componente pesado solo cuando haga falta (dynamic import)
 * - Envuelve con Suspense y un fallback mientras carga
 * - Piensa en el "why": bundles más pequeños, carga inicial más rápida
 */
export function CodeSplitting() {
  return (
    <div className="flex flex-col gap-4">
      {/* Implementa aquí */}
    </div>
  )
}

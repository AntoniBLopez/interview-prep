/**
 * Objetivo: servicios e inyección de dependencias en Angular.
 *
 * - Crea un servicio injectable (conceptualmente: @Injectable({ providedIn: 'root' }))
 * - Inyéctalo en al menos dos componentes
 * - Comparte estado o lógica entre ellos
 */
export function DependencyInjection() {
  return (
    <div className="flex flex-col gap-4">
      {/* Implementa aquí */}
    </div>
  )
}

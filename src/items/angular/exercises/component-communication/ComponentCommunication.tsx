/**
 * Objetivo: comunicación entre componentes padre e hijo en Angular.
 *
 * - @Input() para pasar datos al hijo
 * - @Output() + EventEmitter para enviar datos al padre
 * - Nota: este repo es React; aquí documentas/practicas el patrón Angular
 *   o simulas la estructura mentalmente antes de implementarlo en un proyecto Angular
 */
export function ComponentCommunication() {
  return (
    <div className="flex flex-col gap-4">
      {/* Implementa aquí */}
    </div>
  )
}
